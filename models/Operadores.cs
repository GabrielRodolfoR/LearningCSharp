namespace Operators.models
{
    public class Operadores
    {
        public void Atribuicao()
        { 
            /*
            string a = "15";
            int b = 0;

            int.TryParse(a, out b);
            Console.WriteLine(b);

            /*
            int a = 5;
            double b = a; // Cast pode trocar tipos compativeis, desde que o valor caiba dentro do novo tipo

            Console.WriteLine(b);

            /*
            int inteiro = 5;
            string a = inteiro.ToString(); // tudo pode ser convertido para String

            Console.WriteLine(a);

            
            int a = int.Parse("5"); // Parse transforma o tipo, desde que válido
            int b = Convert.ToInt32("20"); // Aceita Null sem encerrar o programa
            Console.WriteLine(a);
            Console.WriteLine(b);
            
            int a = 10;
            int b = 20;
            int c = a + b;

            c += 5;

            Console.WriteLine($"A: {a}, B: {b}, C: {c}");
             */

        }

    }
}